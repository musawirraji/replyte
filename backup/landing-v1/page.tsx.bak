import type { Metadata } from "next";
import { ReplyteLanding } from "@/features/marketing/ui/ReplyteLanding";

// Root = the Replyte marketing landing page (reproduced verbatim from the
// design handoff). The live demo lives at /[slug].

export const metadata: Metadata = {
  title: "Replyte — Answer every buyer in 8 seconds",
  description:
    "Speed-to-lead for real estate. An AI texts your buyers back instantly, qualifies them, and books the viewing — branded as your agency.",
};

export default function HomePage() {
  return <ReplyteLanding />;
}
